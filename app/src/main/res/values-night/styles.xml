<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:windowBackground">@color/nightRider</item>
        <!-- Custom theme attribute -->
        <item name="snackBarActionTextColor">@color/orange</item>
        <item name="snackBarTextColor">@color/white</item>
        <item name="snackBarBgColor">@color/nightRider</item>

        <item name="fabTint">@color/white</item>
        <item name="fabBgTint">@color/orange</item>

        <item name="editBottomLineColor">@color/orange</item>

        <item name="menuItemTextColor">@color/white</item>
    </style>
</resources>